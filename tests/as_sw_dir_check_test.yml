---

- name: Test Authentication Services software directory check 
  hosts: localhost
  gather_facts: false 
  tasks:
  - name: run the module
    as_chk_clnt_sw_dir:
      sw_dir_path: /var/tmp/as/client 
    register: test_out
  - name: dump module output
    debug:
      msg: '{{ test_out }}'
